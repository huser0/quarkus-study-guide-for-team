@import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&family=Inter:wght@300;400;500;600;700&display=swap');

:root {
  --bg-base: #0d0f14;
  --bg-surface: #13161e;
  --bg-elevated: #1a1e2a;
  --bg-hover: #1f2535;
  --border: #252a38;
  --border-strong: #2e3548;

  --quarkus-red: #e8422a;
  --quarkus-orange: #f97316;
  --spring-green: #6db33f;
  --accent-glow: rgba(232, 66, 42, 0.12);

  --text-primary: #e8eaf0;
  --text-secondary: #8892a4;
  --text-muted: #505870;
  --text-code: #7dd3b0;

  --font-body: 'Inter', system-ui, sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', monospace;

  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;

  --sidebar-width: 280px;
  --header-height: 60px;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }
body {
  font-family: var(--font-body);
  background: var(--bg-base);
  color: var(--text-primary);
  line-height: 1.6;
  font-size: 15px;
  -webkit-font-smoothing: antialiased;
}
::-webkit-scrollbar { width: 5px; height: 5px; }
::-webkit-scrollbar-track { background: var(--bg-base); }
::-webkit-scrollbar-thumb { background: var(--border-strong); border-radius: 10px; }

.app-header {
  position: fixed; top: 0; left: 0; right: 0;
  height: var(--header-height);
  background: rgba(13,15,20,0.92);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--border);
  display: flex; align-items: center;
  padding: 0 24px; z-index: 100; gap: 16px;
}
.logo-q {
  font-family: var(--font-mono); font-weight: 700; font-size: 24px;
  background: linear-gradient(135deg, var(--quarkus-red), var(--quarkus-orange));
  -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
}
.logo-text { font-size: 14px; color: var(--text-secondary); margin-left: 4px; }
.header-sep { width: 1px; height: 24px; background: var(--border); }
.header-subtitle { font-size: 12px; color: var(--text-muted); font-family: var(--font-mono); }
.header-right { margin-left: auto; display: flex; align-items: center; gap: 12px; }
.spring-badge {
  font-size: 11px; color: var(--spring-green);
  background: rgba(109,179,63,0.08); border: 1px solid rgba(109,179,63,0.2);
  border-radius: 20px; padding: 4px 10px; font-family: var(--font-mono);
}
.menu-btn {
  display: none; background: none; border: 1px solid var(--border);
  color: var(--text-secondary); border-radius: var(--radius-sm);
  padding: 6px 9px; cursor: pointer; font-size: 16px; line-height: 1;
}

.app-body { display: flex; padding-top: var(--header-height); min-height: 100vh; }

.sidebar {
  position: fixed; top: var(--header-height); left: 0; bottom: 0;
  width: var(--sidebar-width); background: var(--bg-surface);
  border-right: 1px solid var(--border); overflow-y: auto; z-index: 50;
  transition: transform 0.25s ease;
}
.sidebar-inner { padding: 16px 0 60px; }

.progress-wrap { margin: 12px 20px 6px; }
.progress-label { font-size: 11px; color: var(--text-muted); font-family: var(--font-mono); margin-bottom: 6px; }
.progress-bar { background: var(--border); border-radius: 4px; height: 3px; overflow: hidden; }
.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--quarkus-red), var(--quarkus-orange));
  border-radius: 4px; transition: width 0.4s ease;
}

.sidebar-label {
  font-size: 10px; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase;
  color: var(--text-muted); padding: 16px 20px 6px;
}

.mod-btn { display: block; width: 100%; background: none; border: none; text-align: left; cursor: pointer; padding: 0; }
.mod-header {
  display: flex; align-items: flex-start; gap: 10px; padding: 8px 20px;
  border-left: 2px solid transparent; transition: all 0.15s;
}
.mod-header:hover { background: var(--bg-hover); }
.mod-header.active { border-left-color: var(--quarkus-red); background: var(--accent-glow); }
.mod-num { font-family: var(--font-mono); font-size: 10px; color: var(--text-muted); min-width: 18px; padding-top: 2px; }
.mod-title { font-size: 13px; color: var(--text-secondary); font-weight: 500; line-height: 1.35; }
.mod-header.active .mod-title { color: var(--text-primary); }

.sec-list { padding: 2px 12px 4px 48px; }
.sec-btn {
  display: block; font-size: 12px; color: var(--text-muted); padding: 3px 8px;
  border-radius: 3px; background: none; border: none; text-align: left;
  width: 100%; cursor: pointer; transition: color 0.12s; line-height: 1.5;
}
.sec-btn:hover { color: var(--text-secondary); }
.sec-btn.active { color: var(--quarkus-orange); }

.sidebar-overlay { display: none; position: fixed; inset: 0; background: rgba(0,0,0,0.55); z-index: 40; }

.content-area { margin-left: var(--sidebar-width); flex: 1; }
.content-inner { max-width: 860px; padding: 48px 56px 80px; }

/* Welcome */
.welcome { padding: 48px 56px 80px; max-width: 860px; }
.welcome-tag { font-family: var(--font-mono); font-size: 11px; color: var(--quarkus-red); letter-spacing: 0.1em; text-transform: uppercase; margin-bottom: 14px; }
.welcome-h1 { font-size: 40px; font-weight: 700; line-height: 1.15; letter-spacing: -0.025em; margin-bottom: 16px; }
.welcome-h1 span { background: linear-gradient(135deg, var(--quarkus-red), var(--quarkus-orange)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
.welcome-sub { font-size: 17px; color: var(--text-secondary); line-height: 1.65; max-width: 560px; margin-bottom: 28px; }
.welcome-btns { display: flex; gap: 12px; flex-wrap: wrap; margin-bottom: 48px; }
.btn-primary {
  display: inline-flex; align-items: center; gap: 8px;
  background: linear-gradient(135deg, var(--quarkus-red), var(--quarkus-orange));
  color: white; border: none; border-radius: var(--radius-md);
  padding: 11px 20px; font-size: 14px; font-weight: 600; cursor: pointer;
  transition: opacity 0.15s, transform 0.15s; font-family: var(--font-body);
}
.btn-primary:hover { opacity: 0.9; transform: translateY(-1px); }
.btn-secondary {
  display: inline-flex; align-items: center; gap: 8px;
  background: none; color: var(--text-secondary);
  border: 1px solid var(--border-strong); border-radius: var(--radius-md);
  padding: 11px 20px; font-size: 14px; font-weight: 500; cursor: pointer;
  transition: all 0.15s; font-family: var(--font-body);
}
.btn-secondary:hover { border-color: var(--text-muted); color: var(--text-primary); }

.stats-bar { display: flex; gap: 36px; padding: 24px 0; border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); margin-bottom: 40px; flex-wrap: wrap; }
.stat-val { font-family: var(--font-mono); font-size: 24px; font-weight: 600; background: linear-gradient(135deg, var(--quarkus-red), var(--quarkus-orange)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
.stat-lbl { font-size: 12px; color: var(--text-muted); }

.modules-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 12px; }
.module-card {
  background: var(--bg-surface); border: 1px solid var(--border);
  border-radius: var(--radius-lg); padding: 20px; cursor: pointer;
  transition: all 0.15s; position: relative; overflow: hidden;
}
.module-card::before { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 2px; background: linear-gradient(90deg, var(--quarkus-red), var(--quarkus-orange)); opacity: 0; transition: opacity 0.15s; }
.module-card:hover { border-color: var(--border-strong); background: var(--bg-elevated); transform: translateY(-2px); }
.module-card:hover::before { opacity: 1; }
.card-num { font-family: var(--font-mono); font-size: 11px; color: var(--text-muted); margin-bottom: 8px; }
.card-title { font-size: 14px; font-weight: 600; color: var(--text-primary); line-height: 1.35; margin-bottom: 8px; }
.card-count { font-size: 12px; color: var(--text-muted); }

/* Module view */
.mod-eyebrow { font-family: var(--font-mono); font-size: 11px; color: var(--quarkus-red); letter-spacing: 0.1em; text-transform: uppercase; margin-bottom: 8px; }
.mod-title-h1 { font-size: 30px; font-weight: 700; line-height: 1.2; letter-spacing: -0.02em; margin-bottom: 28px; padding-bottom: 24px; border-bottom: 1px solid var(--border); }

.section-block { margin-bottom: 52px; scroll-margin-top: calc(var(--header-height) + 20px); }
.section-title-h3 {
  font-size: 18px; font-weight: 600; color: var(--text-primary);
  margin-bottom: 20px; display: flex; align-items: center; gap: 10px;
}
.section-title-h3::before { content: ''; display: block; width: 3px; height: 18px; background: linear-gradient(180deg, var(--quarkus-red), var(--quarkus-orange)); border-radius: 2px; flex-shrink: 0; }

/* Markdown */
.md p { color: var(--text-secondary); margin-bottom: 14px; line-height: 1.75; }
.md strong { color: var(--text-primary); font-weight: 600; }
.md ul, .md ol { color: var(--text-secondary); padding-left: 20px; margin-bottom: 14px; }
.md li { margin-bottom: 5px; line-height: 1.7; }
.md blockquote {
  background: rgba(249,115,22,0.06); border-left: 3px solid var(--quarkus-orange);
  border-radius: 0 var(--radius-md) var(--radius-md) 0;
  padding: 13px 18px; margin: 18px 0; color: var(--text-secondary); font-size: 14px;
}
.md blockquote p { margin: 0; }
.md table { width: 100%; border-collapse: collapse; margin: 18px 0; font-size: 13.5px; }
.md th {
  background: var(--bg-elevated); color: var(--text-secondary); font-weight: 600;
  font-size: 11.5px; text-transform: uppercase; letter-spacing: 0.05em;
  padding: 10px 14px; text-align: left; border-bottom: 2px solid var(--border-strong);
}
.md td { padding: 9px 14px; color: var(--text-secondary); border-bottom: 1px solid var(--border); }
.md tr:last-child td { border-bottom: none; }
.md tr:hover td { background: var(--bg-hover); }
.md pre {
  background: var(--bg-elevated); border: 1px solid var(--border);
  border-radius: var(--radius-md); padding: 18px 20px;
  overflow-x: auto; margin: 14px 0;
}
.md code { font-family: var(--font-mono); font-size: 13px; line-height: 1.7; color: var(--text-code); }
.md p code, .md li code {
  background: var(--bg-elevated); border: 1px solid var(--border);
  border-radius: 3px; padding: 2px 6px; font-size: 12.5px; color: var(--quarkus-orange);
}

/* Nav arrows */
.mod-nav-arrows { display: flex; gap: 12px; margin-top: 48px; padding-top: 32px; border-top: 1px solid var(--border); }
.arrow-btn {
  display: flex; align-items: center; gap: 8px;
  background: var(--bg-surface); border: 1px solid var(--border);
  color: var(--text-secondary); border-radius: var(--radius-md);
  padding: 10px 16px; font-size: 13px; cursor: pointer;
  transition: all 0.15s; flex: 1; font-family: var(--font-body);
}
.arrow-btn:hover { border-color: var(--quarkus-red); color: var(--text-primary); }
.arrow-btn.right { justify-content: flex-end; }
.arrow-lbl { font-size: 11px; color: var(--text-muted); display: block; }
.arrow-btn.right .arrow-lbl { text-align: right; }

@media (max-width: 900px) {
  .sidebar { transform: translateX(-100%); }
  .sidebar.open { transform: translateX(0); }
  .sidebar-overlay.open { display: block; }
  .menu-btn { display: flex; align-items: center; }
  .content-area { margin-left: 0; }
  .content-inner, .welcome { padding: 32px 24px 60px; }
  .welcome-h1 { font-size: 28px; }
  .modules-grid { grid-template-columns: 1fr 1fr; }
  .header-subtitle { display: none; }
}
@media (max-width: 560px) {
  .modules-grid { grid-template-columns: 1fr; }
  .stats-bar { gap: 20px; }
}
